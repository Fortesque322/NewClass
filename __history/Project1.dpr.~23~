program Project1;

{$APPTYPE CONSOLE}

{$R *.res}

uses
  System.SysUtils,
  Unit1 in 'Unit1.pas';

var
  Auto : TCar;
  a,v,t : real;
  x : string;
  str: string;
begin
  try
    Auto := TCar.Create(225, SixSpeed, FiveDoors, 'Honda', 'Red', 'Japan', true);   // Почему если тру то -1?
    Writeln('Марка: ' +  Auto.NameCar);
    Writeln('Цвет: ' +  Auto.ColorCar);
    Writeln('Страна: ' +  Auto.CountryCar);
    if Auto.EngineStart = true then
      Writeln('Двигатель заведен')
    else
      Writeln('Двигатель выключен');
    Writeln('Максимальная скорость: ' +  IntToStr(Auto.MaxSpeedCar));
    Write('Для рассчета изменения скорости введите время: ');
    Readln(t);
    a := 5;
    v := (a / t) ;
    Writeln('Разность скоростей между конечной и начальной: ' + FloatToStr(v));
    Writeln('Хотите ввести новые данные? Yes or No');
    Readln(x);
    if x = 'yes' then
      Write('Введите Марку: ');
      Readln(str);
      Auto.SetName(str);
      Write('Введите Цвет:');
      Readln(str);
      Auto.SetColor(str);
      Write('Введите страну: ');
      Readln(str);
      Auto.SetCounty(str);

    Readln;
  except
    on E: Exception do
      Writeln(E.ClassName, ': ', E.Message);
  end;
end.
